# 🌍 GeoApp — Асинхронный геокодер с кешем

**GeoApp** — это консольное Python-приложение, которое позволяет получать координаты по адресу и находить адрес по координатам.  
Программа использует:

- [Dadata.ru](https://dadata.ru/) — для очистки и нормализации адресов
- [Nominatim (OpenStreetMap)](https://nominatim.openstreetmap.org/) — для прямого и обратного геокодирования
- SQLite — для кеширования запросов и снижения нагрузки на внешние API

## 🗂 Структура проекта

```text
geoapp/
│
├── main.py                 # Точка входа
├── services/
│   ├── geocoder.py         # Обработка адресов и координат
│   └── input_handler.py    # Обработка пользовательских команд
├── database/
│   ├── models.py           # SQLAlchemy-модели и инициализация БД
│   └── cache.py            # Кеширование адресов
└── geo.sqlite3             # База данных (создаётся автоматически)
```

## 🚀 Запуск

```bash
python main.py
```

Вы увидите меню:

```
Добро пожаловать в GeoApp!

Введите команду:
1 — Ввести координаты
2 — Ввести адрес
exit — Выход
```

## 🧠 Примеры

### 🔹 Адрес → координаты

```
>> 2
Город: Екатеринбург
Улица: Ленина
Дом: 40

Поиск по адресу: Екатеринбург Ленина 40 Россия
Широта: 56.8377762, Долгота: 60.5974995
```

### 🔹 Координаты → адрес

```
>> 1
Введите широту и долготу через пробел: 56.8378 60.5975

Адрес: Россия, Свердловская область, Екатеринбург, проспект Ленина, 40
```

## 💾 Кеширование

Все успешные запросы сохраняются в SQLite-базу (`geo.sqlite3`). При повторном вводе тех же данных (адреса или координат), результат берётся из кеша без обращения к внешним сервисам.

